# TheLimbo Router Contracts
A set of abstractions extracted out of the TheLimbo components.

## Installation
To install this package you can use [composer](https://getcomposer.org):

```bash
composer require the-limbo/router-contracts
```
